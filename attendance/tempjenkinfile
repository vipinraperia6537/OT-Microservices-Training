pipeline {
    agent any

    stages {
    stage("Building image") {
    steps{
        dir("attendance") {
            docker.build('${JOB_NAME}', '-f Dockerfile .')
            }
        }
        }
        
    }
}
