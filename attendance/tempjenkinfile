pipeline {
    agent any

    stages {
    stage("Building image") {
    steps{
    script{
        dir("attendance") {
          sh "whoami"
          // sh "docker build ."
          
            }
           } 
        }
        }
        
    }
}
