pipeline {
    agent any

    stages {
    stage("Building image") {
    steps{
    script{
        dir("attendance") {
          sh "whoami"
          sh "sudo docker build ."
          
            }
           } 
        }
        }
        stage("Building image") {
    steps{
    script{
        dir("employee") {
          
          sh "sudo docker build ."
          
            }
           } 
        }
        }
        
    }
}
