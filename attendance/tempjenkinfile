pipeline {
    agent any

    stages {
    stage("Building image") {
    steps{
    script{
        dir("attendance") {
          sh "docker.build('${JOB_NAME}', '-f Dockerfile .')"
            }
           } 
        }
        }
        
    }
}
